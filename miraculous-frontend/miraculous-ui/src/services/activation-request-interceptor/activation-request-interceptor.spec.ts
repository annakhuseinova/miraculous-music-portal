import { ActivationRequestInterceptor } from './activation-request-interceptor';

describe('ActivationRequestInterceptor', () => {
  it('should create an instance', () => {
    expect(new ActivationRequestInterceptor()).toBeTruthy();
  });
});
