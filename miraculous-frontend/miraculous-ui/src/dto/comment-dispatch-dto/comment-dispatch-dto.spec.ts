import { CommentDispatchDto } from './comment-dispatch-dto';

describe('CommentDispatchDto', () => {
  it('should create an instance', () => {
    expect(new CommentDispatchDto()).toBeTruthy();
  });
});
