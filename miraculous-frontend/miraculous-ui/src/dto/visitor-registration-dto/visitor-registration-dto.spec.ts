import { VisitorRegistrationDto } from './visitor-registration-dto';

describe('VisitorRegistrationDto', () => {
  it('should create an instance', () => {
    expect(new VisitorRegistrationDto()).toBeTruthy();
  });
});
