import { VisitorAuthorizationDto } from './visitor-authorization-dto';

describe('VisitorAuthorizationDto', () => {
  it('should create an instance', () => {
    expect(new VisitorAuthorizationDto()).toBeTruthy();
  });
});
