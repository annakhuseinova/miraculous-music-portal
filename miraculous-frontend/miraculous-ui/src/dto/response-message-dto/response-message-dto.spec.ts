import { ResponseMessageDto } from './response-message-dto';

describe('ResponseMessageDto', () => {
  it('should create an instance', () => {
    expect(new ResponseMessageDto()).toBeTruthy();
  });
});
