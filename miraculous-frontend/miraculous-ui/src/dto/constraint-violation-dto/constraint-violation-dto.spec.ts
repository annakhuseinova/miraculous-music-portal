import { ConstraintViolationDto } from './constraint-violation-dto';

describe('ConstraintViolationDto', () => {
  it('should create an instance', () => {
    expect(new ConstraintViolationDto()).toBeTruthy();
  });
});
